# ── Build trees (regenerated by `make` / `make release`) ──
build/
release/

# ── Third-party libraries (fetched/built by setup.sh; see THIRD_PARTY.md) ──
UDBM/
utap/
uppaal/

# ── Holding area for files not part of the artifact ──
avoid/

# ── Generated results and figures ──
results/
simulation/results/
assets/time_vs_statespace.pdf
assets/memory_vs_statespace.pdf
assets/time_memory_combined.pdf

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/

# ── Logs / scratch ──
build.log
simulation/sim_run.log

# ── OS / editor ──
.DS_Store
.vscode/
